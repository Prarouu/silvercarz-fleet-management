import { AppContentSkeleton } from '@/components/shared/app-content-skeleton';

/** Route-level loading UI for the authenticated app shell. */
export default function AppLoading() {
  return <AppContentSkeleton />;
}
